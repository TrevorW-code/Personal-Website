// const { TURBO_TRACE_DEFAULT_MEMORY_LIMIT } = require("next/dist/shared/lib/constants");

const options = {
    arrowParens: 'avoid',
    singleQuote: true,
    bracketSpacing: true,
    endOfLine: 'lf',
    semi: false,
    tabWidth: 2,
    trailingComma: 'none'
}

module.exports = options